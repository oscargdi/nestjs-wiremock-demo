# NestJS + Wiremock demo

## Execute service

Run a service by using `make` target:

```zsh
make run SERVICE=<service>
```

For instance, to run `wiremock` service:

```zsh
make run SERVICE=wiremock
```
